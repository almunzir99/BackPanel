using @[ProjectName].Application.Attributes.Permissions;
using @[ProjectName].Application.DTOs;
using @[ProjectName].Application.DTOsRequests;
using @[ProjectName].Application.Interfaces;
using @[ProjectName].Domain.Entities;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using @[ProjectName].WebApplication.Areas.API.Controllers.Common;
using @[ProjectName].Application.Resolvers.UriResolver;

using MediatR;

namespace @[ProjectName].WebApplication.Controllers;

[ApiController]
[Route("api/[controller]")]
public class @[Models]Controller : ApiController<@[Model],@[Model]Dto,@[Model]DtoRequest>
{
    
    public @[Models]Controller(IUriResolver uriService, IMediator mediator) : base(uriService, mediator)
    {
        
    }
    public override string PermissionTitle => "@[Models]Permissions";
}